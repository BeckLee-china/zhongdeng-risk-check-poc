{
  "name": "zhongdeng-risk-check-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "客户中登担保登记核查与内部合同/发票自动比对 POC",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "node --import tsx --test test/**/*.test.ts",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "pdf-parse": "^1.1.1",
    "playwright": "^1.55.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/pdf-parse": "^1.1.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "engines": { "node": ">=22" }
}
